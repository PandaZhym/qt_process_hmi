# Process HMI Framework 架构方案讨论记录

## 项目背景

- **目标**：用 Qt 做一个流程工业通用的 HMI 上层框架，不限于西门子 PLC
- **用户**：自动化工程师，日常接触 PLC（S7-1200）、DCS（和利时）
- **上层应用**：替代 WinCC 风格的 HMI，做更好看的画面
- **DCS**：先不考虑（和利时等比较封闭，没有像 snap7 这样的开放库）

## 前置工作：5 个学习项目

在讨论架构之前，已完成 5 个循序渐进的独立项目，放在 `siemens_plc_comm/` 目录下：

| 项目 | 学什么 | 关键知识点 |
|------|--------|-----------|
| stage1_console_test | snap7 基本连接 | TS7Client、ConnectTo、CMake 链接 DLL |
| stage2_qt_connect | Qt 窗口 + 多线程 | Signal/Slot、QThread、moveToThread |
| stage3_read_data | 读取 PLC 各存储区 | DBRead/MBRead/EBRead/ABRead、大端解析 |
| stage4_write_data | 写入 PLC 并回读验证 | DBWrite/MBWrite/EBWrite/ABWrite、大端打包 |
| stage5_polling | 定时轮询 + 自动重连 | QTimer、QTableWidget、MonitorItem、PlcStatus |

配套文档：每个项目都有《从源码到EXE_小白指南.md/pdf》和《snap7进阶功能指南.md/pdf》。

## 画面方向探索

### qt-custom-widget

开源工业控件库，44 个控件（Valve、Pump、Tank、Pipe、FlowMeter、Gauge、Thermometer 等），兼容现有 QWidget 代码。但 GitHub 拉不下来（网络不通）。

### 自绘方案

用 QPainter 从零创建了 5 个流程工业控件（`process_widgets/`）：

| 控件 | 功能 | 动画 |
|------|------|------|
| TankWidget | 液位百分比、报警色变（红/橙/蓝）、设定值线 | 液位平滑过渡 |
| PumpWidget | 泵体圆圈、三叶旋转叶轮 | 运行时持续旋转 |
| ValveWidget | 三角开度指示（0°~90°）、颜色（绿=开/红=关） | 开度平滑过渡 |
| PipeWidget | 水平/垂直管道、流向动画 | 白色虚线流动 |
| ValueDisplay | 大号数值 + 标签 + 单位 + 报警边框（红/橙/绿） | 无 |

### stage6：控件 + snap7 联调

创建了 stage6_process_hmi，把这些控件和 snap7 对接，演示了 Tank → Pump → Valve 的流程画面。界面整体深色主题，但画面效果不够理想。

## 架构方案讨论

### 最终架构

采用三层架构，数据流如下：

```
┌─ Layer 1: 通讯层 (Snap7Adapter) ──────────────────────┐
│  子线程运行                                              │
│  TS7Client + ReadMultiVars 批量轮询                      │
│  发送 tagValuesReady({"TANK_LEVEL" → 65.2, ...})        │
│  自动重连 + CPU 状态                                     │
├─ Layer 2: 数据模型层 (TagManager) ─────────────────────┤
│  主线程运行                                              │
│  updateTag(name, value, valid) → emit tagChanged()      │
│  纯内存 QHash<QString, TagValue>                        │
├─ Layer 3: 画面层 (Widgets) ────────────────────────────┤
│  TankWidget / PumpWidget / ValveWidget / ValueDisplay   │
│  订阅 TagManager::tagChanged 信号自动刷新                │
└─────────────────────────────────────────────────────────┘
```

数据流向：

```
PLC [DB6.DBD0...]  ←──ReadMultiVars──  Snap7Adapter(子线程)
                                                 │ tagValuesReady() signal
                                                 ▼
                                         TagManager(主线程)
                                           updateTag()
                                           emit tagChanged()
                                                 │
                                                 ▼
                                         画面控件(主线程)
                                    onTagChanged() → 自动刷新
```

### 三个关键决策（全部选 A）

| 决策 | A 路线（已选） | B 路线（后期演进方向） |
|------|---------------|---------------------|
| **画面组态** | 写代码布局（灵活，程序员方式） | JSON 配置文件驱动（适合工程师/最终用户） |
| **通讯协议** | 先深耕 Siemens（Snap7） | 抽象接口 IProtocol，同时支持 Modbus/OPC UA |
| **数据存储** | 纯内存（轻量，重启丢失） | SQLite 持久化（历史趋势/报警记录可查） |

选 A 的理由：先跑通 MVP 验证架构，后期向 B 演进。

### 架构的"可插拔"设计

换 PLC 品牌只需替换 Layer 1，上面两层不动：

```
未来 Modbus 支持：
  ModbusAdapter → tagValuesReady() → TagManager (不变) → Widgets (不变)

未来 OPC UA 支持：
  OpcUaAdapter → tagValuesReady() → TagManager (不变) → Widgets (不变)
```

TagManager 是架构中枢，通讯层和画面层互不感知。

## stage7 MVP 框架实现

### 文件结构

```
qt_process_hmi/
├── siemens_plc_comm/           ← 原有 5 个学习项目
│   ├── stage1_console_test/
│   ├── stage2_qt_connect/
│   ├── stage3_read_data/
│   ├── stage4_write_data/
│   ├── stage5_polling/
│   └── snap7-full-1.4.2/
├── process_widgets/            ← 流程工业控件库（可复用）
│   ├── tank_widget.h/cpp
│   ├── pump_widget.h/cpp
│   ├── valve_widget.h/cpp
│   ├── pipe_widget.h/cpp
│   └── value_display.h/cpp
├── stage6_process_hmi/         ← 控件 + snap7 初次联调
└── stage7_mvp_framework/       ← 当前：MVP 三层架构
    ├── tag_manager.h/cpp       ← Layer 2: 数据中枢
    ├── snap7_adapter.h/cpp     ← Layer 1: 通讯层
    ├── mainwindow.h/cpp        ← 组装 + 画面绑定
    ├── main.cpp
    ├── CMakeLists.txt
    └── build/                  ← 已编译部署
```

### 关键代码逻辑

**连接时的标签映射**（mainwindow.cpp onConnectClicked）：

```cpp
QVector<TagMapping> mappings;
mappings.append({"TANK_LEVEL",    S7AreaDB, 6, 0,  4, 6});  // DB6.DBD0  FLOAT
mappings.append({"PUMP_SPEED",    S7AreaDB, 6, 4,  4, 6});  // DB6.DBD4  FLOAT
mappings.append({"VALVE_OPENING", S7AreaDB, 6, 8,  4, 6});  // DB6.DBD8  FLOAT
mappings.append({"TEMP_VALUE",    S7AreaDB, 6, 12, 4, 6});  // DB6.DBD12 FLOAT
mappings.append({"PRESS_VALUE",   S7AreaDB, 6, 16, 4, 6});  // DB6.DBD16 FLOAT
mappings.append({"FLOW_VALUE",    S7AreaDB, 6, 20, 4, 6});  // DB6.DBD20 FLOAT
```

这里就是可插拔的关键——每个标签描述了"标签名 → PLC 地址"的映射，未来换协议只改这一处。

**数据结构**：

- `TagValue`：{ QVariant value; bool valid; QDateTime timestamp }
- `TagMapping`：{ tagName; area; dbNum; start; size; valType }
- `TagManager`：QHash<QString, TagValue>，`updateTag()` → `emit tagChanged()`
- `Snap7Adapter`：QTimer 周期触发 `ReadMultiVars` → `emit tagValuesReady()`

## PLC 设置

在 TIA Portal 中：

1. 创建 DB6
2. 属性 → 取消"优化的块访问"
3. 添加变量：

| 地址 | 类型 | 用途 |
|------|------|------|
| DBD0 | REAL | 罐体液位 0~100% |
| DBD4 | REAL | 泵转速 0~100% |
| DBD8 | REAL | 阀门开度 0~100% |
| DBD12 | REAL | 温度 °C |
| DBD16 | REAL | 压力 MPa |
| DBD20 | REAL | 流量 m³/h |

4. CPU 属性 → 保护与安全 → 勾选"允许来自远程对象的 PUT/GET 通信"

## 编译运行

```bash
cd qt_process_hmi/stage7_mvp_framework
export PATH="/c/Qt/Tools/CMake_64/bin:/c/Qt/Tools/mingw1310_64/bin:/c/Qt/Tools/Ninja:$PATH"
cmake -B build -G "Ninja" \
      -DCMAKE_CXX_COMPILER=g++ \
      -DCMAKE_PREFIX_PATH=C:/Qt/6.11.0/mingw_64
cmake --build build
cp snap7.dll build/
cd build && windeployqt Stage7_MVP_Framework.exe
./Stage7_MVP_Framework.exe
```

## 待讨论 / 后续方向

- UI 美化（自绘控件效果一般，考虑 QML 或 SVG 方案）
- 配置文件驱动画面（JSON/YAML 描述流程图，运行时解析成图）
- 通讯协议抽象接口 IProtocol（支持 Modbus、OPC UA）
- 历史数据持久化（SQLite 存储趋势数据和报警记录）
- 趋势曲线（TrendChart 控件）
- 报警管理（报警触发条件、报警列表、声音提示）
- 画面导航 / 多画面切换
